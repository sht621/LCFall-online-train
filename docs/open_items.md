# Open Items

- whether manual ROI should stay global or become environment-specific remains open
- all-data final training may still need a deterministic monitor split for model selection
- pose regeneration requires an installed and runnable MMPose environment
- end-to-end data generation has not yet been executed in this workspace, so generated artifact counts are still expected to be zero until the first run
